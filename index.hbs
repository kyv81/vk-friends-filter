<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{htmlWebpackPlugin.options.title}}</title>
    <link href="https://fonts.googleapis.com/css?family=Fira+Sans:300,500&amp;subset=cyrillic" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"  rel="stylesheet">
    <style>
        *, *:before {
            box-sizing: border-box;
        }
        html, body {
            margin: 0;
            width: 100%;
            height: 100%;
            font-family: 'Fira Sans', sans-serif;
            font-size: 16px;
            background: #999;
            overflow: hidden;
        }
        .logo__pic {
            display: block;
            margin: 80px auto;
        }
        .filter {
            position: absolute;
            top: 50%;
            left: 50%;
            -webkit-transform: translate(-50%, -50%);
            -moz-transform: translate(-50%, -50%);
            -ms-transform: translate(-50%, -50%);
            -o-transform: translate(-50%, -50%);
            transform: translate(-50%, -50%);
            overflow: hidden;
        }
        .filter__header {
            width: 100%;
            display: flex;
            background: #ff8663;
            overflow: hidden;
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
            padding: 15px;
            font-size: 16px;
            font-weight: 500;
            color: #fff;
        }
        .filter__search {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            height: 45px;
            padding: 10px 15px;
            background: #f0f0f0;
        }
        .filter__search-field {
            position: relative;
            width: 280px;
            height: 26px;
        }
        .filter__search-field:before {
            position: absolute;
            top: 50%;
            left: 10px;
            transform: translateY(-50%);
            content: "\f002";
            font-family: 'FontAwesome';
            color: #9f9f9f;
            font-weight: 300;
        }
        .filter__search-field + .filter__search-field:before {
            content: "";
        }
        .filter__search-input {
            width: 100%;
            height: 100%;
            padding: 0 10px 0 30px;
            font-weight: 300;
            color: #9f9f9f;
            outline: none;
            border: 0;
            border-radius: 13px;
            font-size: 12px;
        }
        .filter__search-input::placeholder {
            color: #9f9f9f;
        }
        .filter__footer {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            background-color: #f0f0f0;
            width: 100%;
            height: 45px;
            padding: 10px;
            border-radius: 0 0 10px 10px;
        }
        .filter__footer-btn {
            background: #ff8663;
            color: #fff;
            padding: 9px 15px;
            font-size: 12px;
            font-weight: 300;
            -webkit-border-radius: 30px;
            -moz-border-radius: 30px;
            border-radius: 30px;
            cursor: pointer;
            border: 0;
            outline: 0;
        }
        .filter__footer-btn:hover {
            background: #fff;
            color: #ff8663;
        }
        .friends {
            display: flex;
            background: #fff;
            height: 350px;
        }
        .friends__column {
            width: 50%;
            padding: 15px 15px 0;
            overflow: auto;
        }
        .friends__column + .friends__column {
            border-left: 1px solid #f0f0f0;
        }
        .friends__title {
            border-bottom: 1px solid #f0f0f0;
            font-size: 12px;
            color: #4f4f4f;
            font-weight: 500;
            padding-bottom: 5px;
        }
        .friends__list {
            padding: 0;
            margin: 0;
            list-style: none;
        }
        .friends__item {
            border-bottom: 1px solid #f0f0f0;
            padding: 5px;
        }
        .friends__item:hover {
            background-color: #f0f0f0;
        }
        .friend {
            display: flex;
            align-items: center;
            position: relative;
            width: 100%;
            padding-right: 25px;
        }
        .friend__photo {
            display: block;
            width: 45px;
            height: 45px;
            margin-right: 10px;
            border-radius: 50%;
        }
        .friend__name {
            font-size: 14px;
            color: #000;
            font-weight: 500;
            cursor: default;
        }
        .friend__action {
            position: absolute;
            top: 50%;
            right: 5px;
            transform: translateY(-50%);
            display: block;
            font-family: 'FontAwesome';
            font-size: 20px;
            color: #c4c4c4;
            cursor: pointer;
            font-style: normal;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="logo">
            <img class="logo__pic" src="./src/img/logo.png" alt="">
        </div>
        <div class="filter">
            <div class="filter__header">Выберите друзей</div>
            <div class="filter__search">
                <div class="filter__search-field">
                    <input type="text" id="search-left" class="filter__search-input" placeholder="Начните вводить имя друга">
                </div>
                <div class="filter__search-field">
                    <input type="text" id="search-right" class="filter__search-input" placeholder="Название">
                </div>
            </div>
            <div class="friends">
                <div class="friends__column">
                    <div class="friends__title">Ваши друзья</div>
                    <ul id="friends-left-list" class="friends__list"></ul>
                </div>
                <div class="friends__column droppable">
                    <div class="friends__title">Друзья в списке</div>
                    <ul id="friends-right-list" class="friends__list"></ul>
                </div>
            </div>
            <div class="filter__footer">
                <button id="button-save" class="filter__footer-btn">Сохранить</button>
            </div>
        </div>
    </div>
    <script src="https://vk.com/js/api/openapi.js" type="text/javascript"></script>
</body>
</html>
